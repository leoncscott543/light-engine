pub mod particle_view;
pub mod snapshot;

// src/python_bridge/mod.rs

pub fn init() {
    // Setup Python integration here (data stream, embedding, etc.)
}

pub fn stream_particle_snapshot() {
    // Export to numpy or serialized form
}
